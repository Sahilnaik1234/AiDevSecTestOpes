secret_scanners:
  gitleaks:
    install: "sudo apt install gitleaks -y"

sast_scanners:
  semgrep:
    install: "pip install semgrep"

dependency_scanners:
  trivy:
    run: "docker run --rm aquasec/trivy fs ."

compliance_frameworks:
  - hipaa
  - soc2